{
  "name": "dice-roller",
  "private": true,
  "version": "0.1.3",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "node --test",
    "android:sync": "npm run build && cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "8.4.2",
    "@capacitor/core": "8.4.2",
    "aether-dice": "0.3.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.4.2",
    "vite": "^7.3.6"
  }
}
